# name: Playwright Tests (POM_Automation)

# on:
#   push:
#     branches: [ master ]
#     # Only run if changes are made inside the POM_Automation folder
#     paths:
#       - 'POM_Automation/**'
#   pull_request:
#     branches: [ master ]
#     paths:
#       - 'POM_Automation/**'

# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest
    
#     # This ensures all 'run' commands happen inside your subfolder
#     defaults:
#       run:
#         working-directory: ./POM_Automation

#     steps:
#     - uses: actions/checkout@v4

#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
#         cache: 'npm'
#         cache-dependency-path: POM_Automation/package-lock.json

#     - name: Install dependencies
#       run: npm ci

#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps

#     - name: Run Playwright tests
#       run: npx playwright test

#     - uses: actions/upload-artifact@v4
#       if: always()
#       with:
#         name: playwright-report
#         # Path must be from the repository root
#         path: POM_Automation/playwright-report/
#         retention-days: 30


# name: Playwright Tests (POM_Automation)

# on:
#   workflow_dispatch:
#   push:
#     branches: [ master ]
#     paths:
#       - 'POM_Automation/**'
#   pull_request:
#     branches: [ master ]
#     paths:
#       - 'POM_Automation/**'

# # specific permissions required for publishing to GitHub Pages
# permissions:
#   contents: write

# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest
    
#     defaults:
#       run:
#         working-directory: ./POM_Automation

#     steps:
#     - uses: actions/checkout@v4

#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
#         cache: 'npm'
#         cache-dependency-path: POM_Automation/package-lock.json

#     - name: Install dependencies
#       run: npm ci

#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps

#     - name: Run Playwright tests
#       run: npx playwright test
#       continue-on-error: true # Ensure report generates even if tests fail

#     # --- NEW: Allure Report Generation ---
    
#     - name: Get Allure history
#       uses: actions/checkout@v4
#       if: always()
#       continue-on-error: true
#       with:
#         ref: gh-pages
#         path: gh-pages

#     - name: Generate Allure Report
#       uses: simple-elf/allure-report-action@master
#       if: always()
#       with:
#         # path to where playwright saves results (inside your subfolder)
#         allure_results: POM_Automation/allure-results
#         # path to store previous history for trend graphs
#         allure_history: allure-history
#         keep_reports: 20

#     - name: Deploy Report to GitHub Pages
#       uses: peaceiris/actions-gh-pages@v3
#       if: always()
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_branch: gh-pages
#         publish_dir: allure-history


# name: Playwright Tests (POM_Automation)

# on:
#   workflow_dispatch:
#   push:
#     branches: [ master ]
#     paths:
#       - 'POM_Automation/**'
#   pull_request:
#     branches: [ master ]
#     paths:
#       - 'POM_Automation/**'

# permissions:
#   contents: write

# jobs:
#   test:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest
    
#     defaults:
#       run:
#         working-directory: ./POM_Automation

#     steps:
#     - uses: actions/checkout@v4

#     - uses: actions/setup-node@v4
#       with:
#         node-version: lts/*
#         cache: 'npm'
#         cache-dependency-path: POM_Automation/package-lock.json

#     - name: Install dependencies
#       run: npm ci

#     - name: Install Playwright Browsers
#       run: npx playwright install --with-deps

#     - name: Run Playwright tests
#       run: npx playwright test
#       continue-on-error: true

#     - name: Get Allure history
#       uses: actions/checkout@v4
#       if: always()
#       continue-on-error: true
#       with:
#         ref: gh-pages
#         path: gh-pages

#     - name: Generate Allure Report
#       uses: simple-elf/allure-report-action@master
#       if: always()
#       with:
#         allure_results: POM_Automation/allure-results
#         allure_history: allure-history
#         keep_reports: 20

#     - name: Deploy Report to GitHub Pages
#       uses: peaceiris/actions-gh-pages@v3
#       if: always()
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_branch: gh-pages
#         publish_dir: allure-history


name: Playwright Tests (POM_Automation)

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - 'POM_Automation/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'POM_Automation/**'

permissions:
  contents: write

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./POM_Automation

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: 'npm'
        cache-dependency-path: POM_Automation/package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    - name: Run Playwright tests
      run: npx playwright test
      continue-on-error: true

    # --- Allure Report Steps ---

    - name: Get Allure history
      uses: actions/checkout@v4
      if: always()
      continue-on-error: true
      with:
        ref: gh-pages
        path: gh-pages

    - name: Generate Allure Report
      uses: simple-elf/allure-report-action@master
      if: always()
      with:
        allure_results: POM_Automation/allure-results
        allure_history: allure-history
        keep_reports: 20

    - name: Deploy Report to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: always()
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_branch: gh-pages
        publish_dir: allure-history 